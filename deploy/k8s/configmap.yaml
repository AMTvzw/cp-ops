apiVersion: v1
kind: ConfigMap
metadata:
  name: cp-ops-config
data:
  NODE_ENV: "production"
  PORT: "31987"
  DB_CLIENT: "sqlite3"
  DB_FILENAME: "/app/data/cp_ops.sqlite"
  DEFAULT_ROOT_USERNAME: "root"
  # Optional Redis for shared rate limiting:
  # REDIS_URL: "redis://cp-ops-redis:6379"
